node_modules/
dist/
.n8n/

# Control Panel SQLite runtime (run history, recipe-inputs cache, scheduler
# state). Grows per-run; can leak secrets if a user pasted one into an input
# field or an LLM echoed one in its output. NEVER commit.
.cache/

# Secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.idea/
*.suo
*.user

# Logs
*.log
*.txt
!services/**/requirements.txt
logs/

# Java & Spring Boot Build
target/
.mvn/
.gradle/
build/
*.jar
*.war
*.class

# Node.js & Frontend (MCP tools)
dist/
.npm/

# tmp and external
tmp/

# Python
__pycache__/
*.pyc

# Claude Code per-user settings (project-level settings.json is committed)
.claude/settings.local.json

# MCP client config — paths are machine-local. Copy from .mcp.example.json.
.mcp.json

# graphify scan output (build artifact)
graphify-out/
